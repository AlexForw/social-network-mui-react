import { Box } from '@mui/material';
import React from 'react';

const Main = () => {
    return (
        <Box backgroundColor="green" flex={4} p={2}>
                Center
        </Box>
    );
};

export default Main;