import { Box } from '@mui/material';
import React from 'react';
import Post from '../assets/Post'


const Main = () => {
    return (
        <Box flex={4} p={2}>
                <Post />
        </Box>
    );
};

export default Main;