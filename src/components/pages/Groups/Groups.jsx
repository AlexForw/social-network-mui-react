import { Box } from '@mui/material';
import React from 'react';

const Groups = () => {
    return (
        <Box flex={4} p={2} height='100vh'>
            Groups
        </Box>
    );
};

export default Groups;