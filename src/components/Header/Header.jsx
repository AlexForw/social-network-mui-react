import { Box } from '@mui/material';
import React from 'react';

const Header = () => {
    return (
        <Box backgroundColor="blue" p={2}>
          Header  
        </Box>
    );
};

export default Header;